<UserControl x:Class="CoursePlayer.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:CoursePlayer.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:SearchViewModel}"
             Background="{DynamicResource App.Brush.Background}"
             Foreground="{DynamicResource App.Brush.TextPrimary}"
             FontFamily="Segoe UI">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" Style="{StaticResource App.Text.PageTitle}" />

        <TextBox Grid.Row="1"
                 Margin="0,18,0,0"
                 MaxWidth="560"
                 HorizontalAlignment="Left"
                 FontSize="14"
                 Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 materialDesign:HintAssist.Hint="Search courses and lessons"
                 materialDesign:TextFieldAssist.HasLeadingIcon="True"
                 materialDesign:TextFieldAssist.LeadingIcon="Magnify" />

        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:PackIcon Kind="TextSearch"
                                     Width="48"
                                     Height="48"
                                     HorizontalAlignment="Center"
                                     Foreground="{DynamicResource App.Brush.TextDisabled}" />
            <TextBlock Text="{Binding Placeholder}"
                       Margin="0,14,0,0"
                       MaxWidth="420"
                       TextAlignment="Center"
                       Style="{StaticResource App.Text.Body}" />
        </StackPanel>
    </Grid>
</UserControl>
