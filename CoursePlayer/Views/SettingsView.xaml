<UserControl x:Class="CoursePlayer.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:CoursePlayer.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel}"
             Background="{DynamicResource App.Brush.Background}"
             Foreground="{DynamicResource App.Brush.TextPrimary}"
             FontFamily="Segoe UI">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Settings"
                   Style="{StaticResource App.Text.PageTitle}"
                   Margin="0,0,0,20" />

        <TextBlock Grid.Row="1"
                   Text="Theme"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4"
                   Foreground="{DynamicResource App.Brush.TextPrimary}" />

        <ListBox Grid.Row="2"
                 VerticalAlignment="Top"
                 ItemsSource="{Binding Palettes}"
                 SelectedItem="{Binding SelectedPalette, Mode=TwoWay}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,16,16" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Root"
                                        CornerRadius="10"
                                        BorderThickness="2"
                                        BorderBrush="Transparent"
                                        Padding="2">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource App.Brush.Divider}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource App.Brush.Accent}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Width="220" Padding="0" Cursor="Hand" ClipToBounds="True">
                        <StackPanel>
                            <!--
                                A miniature of the app itself: caption bar, sidebar, content
                                area and an accent chip. Bound to the palette's own colours, so
                                each card previews exactly what selecting it produces.
                            -->
                            <Grid Height="96">
                                <Grid.Background>
                                    <SolidColorBrush Color="{Binding Background}" />
                                </Grid.Background>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="22" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="54" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Caption bar, spanning the full width like the real one -->
                                <Border Grid.Row="0" Grid.ColumnSpan="2">
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding Chrome}" />
                                    </Border.Background>
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Margin="0,0,8,0">
                                        <Ellipse Width="6" Height="6" Margin="3,0">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding TextSecondary}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <Ellipse Width="6" Height="6" Margin="3,0">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding TextSecondary}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <Ellipse Width="6" Height="6" Margin="3,0">
                                            <Ellipse.Fill>
                                                <SolidColorBrush Color="{Binding TextSecondary}" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                    </StackPanel>
                                </Border>

                                <!-- Sidebar, with the selected row picked out -->
                                <Border Grid.Row="1" Grid.Column="0">
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding Surface}" />
                                    </Border.Background>
                                    <StackPanel Margin="6,8,6,0">
                                        <Border Height="9" CornerRadius="2" Margin="0,0,0,4">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding Selected}" />
                                            </Border.Background>
                                        </Border>
                                        <Border Height="9" CornerRadius="2" Margin="0,0,0,4" Opacity="0.5">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding SurfaceRaised}" />
                                            </Border.Background>
                                        </Border>
                                        <Border Height="9" CornerRadius="2" Opacity="0.5">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding SurfaceRaised}" />
                                            </Border.Background>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <!-- Content: a course card and an accent chip -->
                                <Grid Grid.Row="1" Grid.Column="1" Margin="8,8,8,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" CornerRadius="3">
                                        <Border.Background>
                                            <SolidColorBrush Color="{Binding SurfaceRaised}" />
                                        </Border.Background>
                                    </Border>
                                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                                        <Border Width="34" Height="10" CornerRadius="5">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding Accent}" />
                                            </Border.Background>
                                        </Border>
                                        <Border Width="18" Height="10" CornerRadius="5" Margin="4,0,0,0">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding AccentDim}" />
                                            </Border.Background>
                                        </Border>
                                        <Border Width="10" Height="10" CornerRadius="5" Margin="4,0,0,0">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding Warning}" />
                                            </Border.Background>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Grid>

                            <!-- Palette name, on the palette's own surface -->
                            <Border Padding="12,10">
                                <Border.Background>
                                    <SolidColorBrush Color="{Binding Surface}" />
                                </Border.Background>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis">
                                    <TextBlock.Foreground>
                                        <SolidColorBrush Color="{Binding TextPrimary}" />
                                    </TextBlock.Foreground>
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </materialDesign:Card>
                </DataTemplate>
            </ListBox.ItemTemplate>
</ListBox>
</Grid>
</UserControl>

